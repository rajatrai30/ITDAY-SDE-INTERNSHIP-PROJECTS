import { _transition, _transitionDirection } from "../../interfaces/utils/button.interface"

export const animate = (
  transition?: _transition,
  transitionDirection?: _transitionDirection
) => {
  let classNames = ''



  return classNames
}