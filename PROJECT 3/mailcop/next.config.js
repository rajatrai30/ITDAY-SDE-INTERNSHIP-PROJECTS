/** @type {import('next').NextConfig} */
// import { Poppins } from "next/font/google";

const nextConfig = {
  experimental: {
    appDir: true,
  },
}

// const inter = Poppins({
//   subsets: ["latin"],
//   display: "fallback",
// });

module.exports = nextConfig
